export const dynamic = "force-dynamic";
export default function AdminSettingsPage() { const checks = [["Database", "D1 binding: DB"], ["Admin access", process.env.ADMIN_EMAILS ? "Email allowlist configured" : "Owner-only mode: any signed-in viewer"], ["Email", process.env.RESEND_API_KEY ? "Resend configured" : "Awaiting RESEND_API_KEY"], ["Spam protection", process.env.TURNSTILE_SECRET_KEY ? "Turnstile configured" : "Awaiting Turnstile keys"], ["Analytics", process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID ? "GA4 configured" : "Awaiting measurement ID"], ["Lead webhook", process.env.LEAD_WEBHOOK_URL ? "Connected" : "Optional, not configured"]]; return <main className="admin-page"><div className="admin-heading"><div><p>CONFIGURATION</p><h1>Settings</h1></div></div><section className="settings-list">{checks.map(([name,status]) => <div key={name}><strong>{name}</strong><span>{status}</span></div>)}</section><p className="admin-help">Sensitive values are managed as production environment variables and are never displayed here.</p></main>; }
